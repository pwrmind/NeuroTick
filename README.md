# NeuroTick
Bionic Living Graph: Neural Architecture Search via Cellular AutomataBionic Living Graph — это экспериментальный нейроморфный алгоритм классификации, вдохновленный игрой «Жизнь» Джона Конвея и принципами биологической нейропластичности.

В отличие от классических нейросетей, здесь нет фиксированных слоев и обратного распространения ошибки (backpropagation). Архитектура сети «прорастает» сама в процессе взаимодействия с данными, формируя замкнутую динамическую систему.🧠 

**Суть алгоритма**
Программа представляет собой направленный граф, где веса ребер — это «живые клетки». Жизнь каждой связи регулируется тремя факторами:Биологическая пластичность (LTP/LTD): Полезные связи, которые привели к правильному ответу, получают «иммунитет» и усиливаются. Ошибочные связи немедленно наказываются и ослабляются.Гомеостаз (Synaptic Scaling): Каждый нейрон имеет жесткий энергетический бюджет. Если в узел входит слишком много связей, их суммарный вес принудительно нормируется. Это заставляет сеть быть разреженной (sparse) и эффективной.Фазы консолидации («Сон»): Алгоритм периодически входит в режим «сна», отключая мутации. В этот период выживают только самые устойчивые связи с высоким иммунитетом, а эволюционный «мусор» отмирает.🚀

**Ключевые особенности**

Zero-Backprop: Обучение происходит через локальные правила выживания связей, а не через глобальный градиентный спуск.Замкнутые циклы: Сеть поддерживает рекуррентность. Сигнал может циркулировать по петлям, создавая кратковременную динамическую память.

Self-Designing Topology: Алгоритм сам решает, сколько скрытых нейронов использовать и как их связать. Вы подаете данные, а сеть «рисует» мозг.Интеграция с Mermaid: Возможность мгновенной визуализации «карты памяти» обученной сети в виде Markdown-диаграммы.📊

**Сильные и слабые стороныСильные стороны**

Экстремальная легкость: Алгоритм работает на чистом NumPy. Нет необходимости в GPU, PyTorch или TensorFlow.Интерпретируемость: Вы можете буквально видеть, какие пиксели (в задаче Digits) или признаки (в задаче Wine) сеть считает важными, просто взглянув на граф.Устойчивость к малым выборкам: За счет механизма мутаций сеть быстро находит рабочие решения даже на небольших датасетах.

Работа на маломощных системах: Идеально подходит для Edge-вычислений и микроконтроллеров. Память потребляется только под матрицу смежности (sparse matrix).

**Слабые стороны**

Скорость сходимости: Требуется больше тактов «погружения» (tick) для обработки одного образца по сравнению с классическим Forward Pass.

Масштабируемость: На огромных входных данных (например, HD-изображения) матрица весов \(N\times N\) становится слишком разреженной и сложной для вычислений без специальных оптимизаций.

Вероятностная природа: Из-за случайных мутаций архитектура сети может меняться от запуска к запуску (хотя итоговая точность остается стабильной).🛠

Запуск и использованиеАлгоритм полностью совместим с экосистемой scikit-learn.
Требования:
Python 3.10+NumPyScikit-learn (только для загрузки датасетов и препроцессинга)

bash# Установка зависимостей через uv или pip
pip install numpy scikit-learn

**Use code with caution**
Пример обучения на рукописных цифрах:Запустите main.py. В процессе обучения вы увидите лог:Связей: Количество активных синапсов в «мозгу».Мутации: Текущая интенсивность поиска новых решений.Статус: OK/FAIL для текущего образца данных.📈 ВизуализацияПосле завершения обучения программа выводит код для Mermaid.js. Скопируйте его в Mermaid Live Editor, чтобы увидеть архитектуру вашего «цифрового организма».Экспериментальная модель, созданная в рамках исследования альтернативных методов машинного обучения (2025).
